---
# Ports open on serverb
firewall_ports:
  - { port: 2049, proto: tcp }  # NFS
  - { port: 3306, proto: tcp }  # MySQL (internal only — restrict at firewall level)

# rsyslog role: this host forwards logs
rsyslog_role: forwarder
