# Vault password — never commit (lives only on the control node)
vars/secrets.yml

# Generated per-user SSH private keys — never commit (public .pub files are fine)
files/ssh_keys/*

# Ansible runtime artefacts
*.retry
.ansible/
*.log

# Ansible tests
tests/static_checks.sh

# Editor / OS noise
*.swp
*~
.DS_Store
.vscode/

