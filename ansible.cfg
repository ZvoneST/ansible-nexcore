[defaults]
inventory           = inventory.ini
remote_user         = zvone
vault_password_file = /home/zvone/.vault_pass
roles_path          = roles
host_key_checking   = false
retry_files_enabled = false

[privilege_escalation]
become        = true
become_method = sudo
become_user   = root
